<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{titulo}} | PowerBI Ops</title>

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>⚡</text></svg>">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- Google Fonts - Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'system-ui', 'sans-serif'],
                    },
                }
            }
        }
    </script>

    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <!-- Tom Select -->
    <link href="https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/css/tom-select.css" rel="stylesheet">
    
    <style>
        /* Tom Select Custom Styles */
        .ts-wrapper.single .ts-control {
            border-radius: 0.75rem;
            border: 1px solid #e2e8f0;
            padding: 0.625rem 1rem;
            font-size: 0.875rem;
            box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            background: white;
        }
        .ts-wrapper.single .ts-control:focus {
            border-color: #818cf8;
            box-shadow: 0 0 0 3px rgba(129, 140, 248, 0.2);
        }
        .ts-dropdown {
            border-radius: 0.75rem;
            border: 1px solid #e2e8f0;
            box-shadow: 0 10px 25px -5px rgb(0 0 0 / 0.1);
            margin-top: 4px;
        }
        .ts-dropdown .option {
            padding: 0.625rem 1rem;
            border-radius: 0.5rem;
            margin: 0.25rem;
        }
        .ts-dropdown .option.active {
            background: #eef2ff;
            color: #4f46e5;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #f1f5f9;
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb {
            background: #cbd5e1;
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #94a3b8;
        }

        /* Smooth transitions */
        * {
            scroll-behavior: smooth;
        }

        /* Background pattern */
        .bg-pattern {
            background-color: #f8fafc;
            background-image: 
                radial-gradient(at 100% 0%, rgba(139, 92, 246, 0.05) 0%, transparent 50%),
                radial-gradient(at 0% 100%, rgba(6, 182, 212, 0.05) 0%, transparent 50%);
        }
    </style>
</head>

<body class="bg-pattern min-h-screen font-sans text-slate-900 antialiased">
    
    {{!-- Header Premium --}}
    {{> layout/header}}

    {{!-- Main Content --}}
    <main class="pb-8 p-4">
        {{{body}}}
    </main>

    {{!-- Footer --}}
    <footer class="border-t border-slate-200 bg-white/50 backdrop-blur-sm mt-auto">
        <div class="container mx-auto px-6 py-4">
            <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
                <div class="flex items-center gap-2 text-sm text-slate-500">
                    <i class="fa-solid fa-bolt text-violet-500"></i>
                    <span>PowerBI Ops</span>
                    <span class="text-slate-300">•</span>
                    <span>Data Operations Platform</span>
                </div>
                <div class="text-xs text-slate-400">
                    © 2026 · Built with <i class="fa-solid fa-heart text-red-400"></i>
                </div>
            </div>
        </div>
    </footer>

</body>

</html>
